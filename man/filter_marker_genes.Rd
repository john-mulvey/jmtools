% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_type_enrichment.R
\name{filter_marker_genes}
\alias{filter_marker_genes}
\title{Filter marker genes for cell type enrichment analysis}
\usage{
filter_marker_genes(
  marker_genes,
  min_avg_log2fc = 0.25,
  min_auc = 0.8,
  max_padj = 0.05,
  min_pct_1 = 0.1,
  cluster_col = "cluster",
  gene_col = "gene",
  log2fc_col = "avg_log2fc",
  auc_col = "my_auc",
  padj_col = "padj",
  pct1_col = "pct_1"
)
}
\arguments{
\item{marker_genes}{Data frame of raw marker genes containing at minimum a
cluster column, a gene symbol column, and the four QC columns described
below.}

\item{min_avg_log2fc}{Minimum average log2 fold change. Default: 0.25.}

\item{min_auc}{Minimum AUC (area under the ROC curve). AUC > 0.8 is the
primary positive-marker filter. Default: 0.8.}

\item{max_padj}{Maximum adjusted p-value. Default: 0.05.}

\item{min_pct_1}{Minimum fraction of cells in the cluster expressing the
gene. Avoids markers driven by a tiny minority of cells. Default: 0.10.}

\item{cluster_col}{Column containing cell type labels. Default: \code{"cluster"}.}

\item{gene_col}{Column containing gene symbols. Default: \code{"gene"}.}

\item{log2fc_col}{Column containing average log2 fold change. Default:
\code{"avg_log2fc"}.}

\item{auc_col}{Column containing the AUC (area under the ROC curve), as
output by \code{Seurat::FindAllMarkers(test.use = "roc")}. Default: \code{"my_auc"}.}

\item{padj_col}{Column containing adjusted p-values. Default: \code{"padj"}.}

\item{pct1_col}{Column containing the fraction of cells expressing the gene.
Default: \code{"pct_1"}.}
}
\value{
A data frame of the same structure as \code{marker_genes}, retaining only
rows that pass all thresholds.
}
\description{
Applies quality thresholds to a raw single-cell marker gene data frame,
returning only high-confidence positive markers. The filtered output is
intended to be passed to \code{\link[=plot_celltype_effect_sizes]{plot_celltype_effect_sizes()}} and
\code{\link[=plot_celltype_dotplot]{plot_celltype_dotplot()}}, and used to build gene sets for GSEA or ORA.
}
\details{
The \code{marker_genes} data frame can be as generated by
\code{Seurat::FindAllMarkers()}.

Pre-processed marker gene CSVs for the Human Heart Cell Atlas v2 are
available on the group drive at:
\itemize{
\item Level 1 (13 cell types):
\code{/Volumes/groupdir/SUN-BMI-CardiacProteomics/scRNAseq_datasets/JM/atlas_snRNAseq/results/heart_cell_atlas_v2/heart_cell_atlas_v2_all_cells_annotated_predicted_celltype_l1_marker_genes.csv}
\item Level 2 (finer annotations):
\code{/Volumes/groupdir/SUN-BMI-CardiacProteomics/scRNAseq_datasets/JM/atlas_snRNAseq/results/heart_cell_atlas_v2/heart_cell_atlas_v2_all_cells_annotated_predicted_celltype_l2_marker_genes.csv}
}
}
\examples{
\dontrun{
marker_genes_raw <- read.csv("heart_cell_atlas_v2_l1_marker_genes.csv")
markers_filtered <- filter_marker_genes(marker_genes_raw)
}

}
\seealso{
\code{\link[=plot_celltype_effect_sizes]{plot_celltype_effect_sizes()}}, \code{\link[=plot_celltype_dotplot]{plot_celltype_dotplot()}},
\code{\link[=plot_gsea_bars]{plot_gsea_bars()}}
}
