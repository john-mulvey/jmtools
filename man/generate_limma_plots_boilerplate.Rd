% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boilerplate.R
\name{generate_limma_plots_boilerplate}
\alias{generate_limma_plots_boilerplate}
\title{Generate limma results plotting boilerplate code}
\usage{
generate_limma_plots_boilerplate(
  results_name = "results",
  feature_col = "feature_id",
  logfc_col = "log_fc",
  pval_col = "p_value",
  adj_pval_col = "adj_p_value",
  avg_expr_col = "ave_expr",
  signif_col = "diff_abundant",
  title = "Differential Abundance"
)
}
\arguments{
\item{results_name}{Character string specifying the name of the results
data frame variable (output from limma analysis). Default: "results".}

\item{feature_col}{Character string specifying the column containing feature
identifiers (e.g., gene symbols, protein IDs). Default: "feature_id".}

\item{logfc_col}{Character string specifying the column containing log
fold-change values. Default: "log_fc".}

\item{pval_col}{Character string specifying the column containing raw
p-values. Default: "p_value".}

\item{adj_pval_col}{Character string specifying the column containing
adjusted p-values. Default: "adj_p_value".}

\item{avg_expr_col}{Character string specifying the column containing
average expression values. Default: "ave_expr".}

\item{signif_col}{Character string specifying the column containing the
logical significance flag. Default: "diff_abundant".}

\item{title}{Character string for plot titles. Default: "Differential Abundance".}
}
\value{
Returns \code{invisible(NULL)}. The function prints code to the console
which can be copied into an analysis script.
}
\description{
Outputs boilerplate R code for visualising limma differential expression/
abundance results. Includes volcano plot, MA plot, and p-value histogram.
}
\details{
The generated code creates three diagnostic plots:

\enumerate{
\item \strong{Volcano plot}: -log10(p-value) vs log fold-change, with
significant features highlighted and optional gene labels
\item \strong{MA plot}: Log fold-change vs average expression, useful for
checking intensity-dependent bias
\item \strong{P-value histogram}: Distribution of raw p-values, which should
be uniform under the null with enrichment near zero
}

The code includes a \code{genes_to_label} vector that can be customised to
annotate specific features of interest on the volcano and MA plots.
}
\examples{
# Generate plotting boilerplate with default settings
generate_limma_plots_boilerplate()

# Customise for specific column names
generate_limma_plots_boilerplate(
  results_name = "results_contrast",
  feature_col = "protein_id",
  title = "PPCM vs Control"
)
}
