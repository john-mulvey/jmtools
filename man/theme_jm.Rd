% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{theme_jm}
\alias{theme_jm}
\title{Custom minimal theme with axes}
\usage{
theme_jm(
  base_size = NULL,
  base_family = "sans",
  facet = FALSE,
  publication = FALSE
)
}
\arguments{
\item{base_size}{Base font size. Defaults to 11 for regular plots, or 6 when
\code{publication = TRUE}.}

\item{base_family}{Base font family (default: "sans")}

\item{facet}{Logical. If \code{TRUE}, applies facet styling with panel borders
and rotated x-axis labels (default: \code{FALSE})}

\item{publication}{Logical. If \code{TRUE}, uses smaller font sizes suitable for
publication figures (default: \code{FALSE})}
}
\value{
A ggplot2 theme object
}
\description{
A professional-looking ggplot2 theme based on \code{theme_minimal()} with black
axis lines and ticks added back. Optionally adds styling suitable for
faceted plots or publication-ready figures.
}
\examples{
library(ggplot2)

# Regular plot with default font sizes
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_jm()

# Publication-ready plot with smaller fonts
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_jm(publication = TRUE)

# Faceted plot with panel borders
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  facet_wrap(~cyl) +
  theme_jm(facet = TRUE)

}
