% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boilerplate.R
\name{generate_pca_boilerplate}
\alias{generate_pca_boilerplate}
\title{Generate PCA boilerplate code}
\usage{
generate_pca_boilerplate(
  data_name,
  metadata_name,
  colour_var = "group",
  id_col = "sample_id",
  n_pcs_scree = 10,
  n_pcs_pairs = 5,
  scale = TRUE,
  center = TRUE
)
}
\arguments{
\item{data_name}{Character string specifying the name of the data matrix
variable in the generated code.}

\item{metadata_name}{Character string specifying the name of the metadata
data frame variable.}

\item{colour_var}{Character string specifying the metadata column to use
for colouring points (default: "group").}

\item{id_col}{Character string specifying the column in metadata containing
sample identifiers that match the column names of the data matrix
(default: "sample_id").}

\item{n_pcs_scree}{Number of principal components to show in the screeplot
(default: 10).}

\item{n_pcs_pairs}{Number of principal components to include in the ggpairs
plot (default: 5).}

\item{scale}{Logical indicating whether to scale variables to unit variance
in the generated code (default: TRUE).}

\item{center}{Logical indicating whether to centre variables in the
generated code (default: TRUE).}
}
\value{
Returns \code{invisible(NULL)}. The function prints code to the console
which can be copied into an analysis script.
}
\description{
Outputs boilerplate R code for performing PCA on a matrix with features
(e.g., proteins, genes) as rows and samples as columns. The generated code
includes PCA computation, scores plot, screeplot with cumulative variance,
and pairwise component scatterplots.
}
\details{
The generated code uses \code{prcomp()} from base R for PCA computation, avoiding
additional package dependencies. The code includes:

\enumerate{
\item Data preparation (transposing so samples are rows)
\item PCA computation with \code{prcomp()}
\item Scores plot (PC1 vs PC2) coloured by a metadata variable
\item Screeplot showing per-component variance (bars) and cumulative
variance (points/line on secondary y-axis)
\item Pairwise scatterplot matrix of the first N components using
\code{GGally::ggpairs()}
}
}
\examples{
# Generate boilerplate for proteomics analysis
generate_pca_boilerplate(
  data_name = "abundance",
  metadata_name = "sample_meta"
)

# Customise variable names
generate_pca_boilerplate(
  data_name = "protein_matrix",
  metadata_name = "clinical_data",
  colour_var = "treatment"
)
}
