% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda.R
\name{pheatmap_clustered_with_na}
\alias{pheatmap_clustered_with_na}
\title{Clustered heatmap with NA-tolerant distance computation}
\usage{
pheatmap_clustered_with_na(
  mat,
  scale_rows = TRUE,
  clustering_method = "ward.D2",
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  na_col = "white",
  ...
)
}
\arguments{
\item{mat}{Numeric matrix with features as rows and samples as columns.
May contain NA values.}

\item{scale_rows}{Logical. If \code{TRUE} (default), rows are z-score scaled
before computing distances and plotting. Scaling uses \code{\link[base:scale]{base::scale()}} which
ignores NAs. The heatmap is then drawn with \code{scale = "none"} so that
clustering and display are consistent.}

\item{clustering_method}{Agglomeration method passed to \code{\link[stats:hclust]{stats::hclust()}}.
Default: \code{"ward.D2"}.}

\item{cluster_rows}{Logical. Whether to cluster rows. Default: \code{TRUE}.}

\item{cluster_cols}{Logical. Whether to cluster columns. Default: \code{TRUE}.}

\item{na_col}{Colour used for NA cells. Default: \code{"white"}.}

\item{...}{Additional arguments passed to \code{\link[pheatmap:pheatmap]{pheatmap::pheatmap()}}, e.g.
\code{annotation_col}, \code{show_rownames}, \code{main}, \code{fontsize_col}.}
}
\value{
Invisibly returns the \code{pheatmap} object.
}
\description{
A thin wrapper around \code{\link[pheatmap:pheatmap]{pheatmap::pheatmap()}} for the specific case where the
abundance matrix contains NA values and you want to cluster rows and/or
columns. In all other cases, use \code{\link[pheatmap:pheatmap]{pheatmap::pheatmap()}} directly.
}
\details{
Standard \code{pheatmap} fails when clustering rows or columns that contain NAs
because \code{hclust} cannot handle NA distances. This function computes pairwise
Euclidean distances using \code{\link[cluster:daisy]{cluster::daisy()}}, which uses only shared non-NA
values for each pair, then passes pre-computed \code{hclust} objects to
\code{pheatmap}. NA cells are rendered in a distinct colour (default white).

When two rows (or columns) share no non-NA values, \code{daisy} returns NA for
that pair. These NA distances are replaced with the maximum observed distance
so that \code{hclust} can proceed; such features are placed at the periphery of
the dendrogram.
}
\examples{
\dontrun{
pheatmap_clustered_with_na(
  abundance_filt[sig_proteins, ],
  annotation_col = sample_annotation,
  show_rownames = FALSE,
  main = "Differentially abundant proteins"
)
}

}
