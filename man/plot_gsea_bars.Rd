% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsea.R
\name{plot_gsea_bars}
\alias{plot_gsea_bars}
\title{Plot enrichment results as a bar chart}
\usage{
plot_gsea_bars(
  enrichment_results,
  gene_set_col = "gene_set",
  score_col = "nes",
  adj_pval_col = "adj_p_value",
  n_top = NULL,
  adj_p_threshold = NULL,
  x_label = "Enrichment Score",
  title = "Enrichment Results",
  wrap_labels = NULL
)
}
\arguments{
\item{enrichment_results}{Data frame of enrichment results, e.g. from
the GSEA/ORA boilerplate functions.}

\item{gene_set_col}{Column containing gene set or cell type labels. Default:
\code{"gene_set"}. Use \code{"cell_type"} for cell type enrichment results.}

\item{score_col}{Column containing the enrichment score. Default: \code{"nes"}
(GSEA). Use \code{"enrichment_score"} for ORA results.}

\item{adj_pval_col}{Column containing adjusted p-values for significance
annotation. Default: \code{"adj_p_value"}.}

\item{n_top}{Maximum number of rows to show, split evenly between enriched
and depleted. \code{NULL} (default) shows all rows.}

\item{adj_p_threshold}{Adjusted p-value threshold for filtering. \code{NULL}
(default) shows all rows regardless of significance.}

\item{x_label}{X-axis label. Default: \code{"Enrichment Score"}.}

\item{title}{Plot title. Default: \code{"Enrichment Results"}.}

\item{wrap_labels}{Number of characters at which to wrap long labels. \code{NULL}
(default) disables wrapping.}
}
\value{
A ggplot2 object.
}
\description{
Creates a horizontal bar chart of enrichment scores from \code{\link[fgsea:fgsea]{fgsea::fgsea()}}
or \code{\link[fgsea:fora]{fgsea::fora()}} results. Bars are coloured by a continuous blue-white-red
gradient and annotated with significance stars. Works with both GSEA and ORA
output, and with cell type or pathway-level results.
}
\examples{
\dontrun{
# Pathway GSEA - show only significant, top 20
plot_gsea_bars(gsea_results, n_top = 20, adj_p_threshold = 0.05,
               wrap_labels = 40,
               x_label = "Normalised Enrichment Score (NES)")
}

}
\seealso{
\code{\link[=plot_gsea_dots]{plot_gsea_dots()}}, \code{\link[=plot_clustered_enrichment_bars]{plot_clustered_enrichment_bars()}},
\code{\link[=filter_marker_genes]{filter_marker_genes()}}
}
