% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_type_enrichment.R
\name{plot_celltype_dotplot}
\alias{plot_celltype_dotplot}
\title{Plot a marker gene dot plot by cell type}
\usage{
plot_celltype_dotplot(
  marker_genes,
  genes,
  cluster_col = "cluster",
  gene_col = "gene",
  pct1_col = "pct_1",
  avg_expr_col = "avg_expr",
  title = "Top cell type marker genes"
)
}
\arguments{
\item{marker_genes}{Data frame of \strong{raw} (unfiltered) marker genes,
containing expression data for all cell types. Used only for looking up
expression values; gene selection is controlled by \code{genes}.}

\item{genes}{Character vector of gene symbols to plot, in the desired
left-to-right display order on the x-axis. Typically derived from a
filtered marker gene data frame, e.g. via \code{\link[=filter_marker_genes]{filter_marker_genes()}} and
manual selection of top markers per cell type.}

\item{cluster_col}{Column in \code{marker_genes} for cell type labels. Default:
\code{"cluster"}.}

\item{gene_col}{Column in \code{marker_genes} for gene symbols. Default: \code{"gene"}.}

\item{pct1_col}{Column for fraction of cells expressing the gene. Default:
\code{"pct_1"}.}

\item{avg_expr_col}{Column for average expression (used for colour scaling).
Default: \code{"avg_expr"}.}

\item{title}{Plot title. Default: \code{"Top cell type marker genes"}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a dot plot of a supplied set of marker genes, where dot size encodes
the fraction of cells expressing the gene and dot colour encodes scaled
average expression. Expression is scaled per gene across all cell types
present in \code{marker_genes}, so the colour scale reflects the full range of
expression.
}
\examples{
\dontrun{
markers_filtered <- filter_marker_genes(marker_genes_raw)
top_genes <- tapply(markers_filtered$gene, markers_filtered$cluster,
                    function(g) head(g, 5))
genes_to_plot <- unique(unlist(top_genes))
plot_celltype_dotplot(marker_genes_raw, genes = genes_to_plot)
}

}
\seealso{
\code{\link[=filter_marker_genes]{filter_marker_genes()}}, \code{\link[=plot_gsea_bars]{plot_gsea_bars()}}
}
