% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boilerplate.R
\name{generate_qc_boilerplate}
\alias{generate_qc_boilerplate}
\title{Generate proteomics QC boilerplate code}
\usage{
generate_qc_boilerplate(
  data_name = "abundance",
  metadata_name = "sample_meta",
  id_col = "sample_id",
  group_var = "group",
  precursor_data_name = NULL,
  precursor_id_col = "Precursor.Id"
)
}
\arguments{
\item{data_name}{Character string specifying the name of the abundance
matrix variable (features as rows, samples as columns).
Default: \code{"abundance"}.}

\item{metadata_name}{Character string specifying the name of the metadata
data frame variable. Must contain \code{id_col} and \code{group_var} columns.
Default: \code{"sample_meta"}.}

\item{id_col}{Character string specifying the column in metadata containing
sample identifiers that match the column names of the data matrix.
Default: \code{"sample_id"}.}

\item{group_var}{Character string specifying the metadata column used for
colouring plots. Default: \code{"group"}.}

\item{precursor_data_name}{Character string giving the name of the
long-format precursor data frame (one row per precursor per sample),
from which per-sample precursor counts are aggregated. Must contain
columns \code{id_col} and \code{precursor_id_col}. Set to \code{NULL} to omit the
precursor count plot (e.g. when working from a pre-processed protein
matrix without precursor-level data). Default: \code{NULL}.}

\item{precursor_id_col}{Character string specifying the column in
\code{precursor_data_name} that identifies individual precursors.
Default: \code{"Precursor.Id"}.}
}
\value{
Returns \code{invisible(NULL)}. The function prints code to the console
which can be copied into an analysis script.
}
\description{
Outputs boilerplate R code for a standard proteomics QC section. The
generated code covers identification counts, missing value analysis, data
completeness, sample correlation, and within-group CV - all plotted without
titles and using a consistent abundance matrix plus \code{sample_meta} object.
}
\details{
The generated code assumes the abundance matrix is already log2-transformed
before the correlation and CV sections; add a log2 transformation step if
needed. The CV is back-computed to the linear scale (\code{2^abundance}) so that
it reflects the original intensity distribution.

The missing value heatmap uses \code{pheatmap} and excludes proteins that are
observed in every sample (i.e. have no missing values), so only informative
rows are shown.

The per-protein missingness-vs-abundance plot shows each protein's detection
rate against its mean observed log2 abundance, with a \code{quasibinomial} GLM
trend. A negative trend indicates left-censored / MNAR missingness and a flat
trend indicates MCAR, which informs the choice of imputation method. Run this
on the pre-imputation matrix (before any per-protein completeness filter,
normalisation or imputation).

The sample correlation heatmap uses \code{ggcorrplot} with Pearson correlation
computed on pairwise complete observations.
}
\examples{
# Generate QC boilerplate with default names, no precursor plot
generate_qc_boilerplate()

# Include precursor counts from a DIA-NN long-format table
generate_qc_boilerplate(
  precursor_data_name = "df_raw",
  precursor_id_col    = "Precursor.Id"
)
}
