% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boilerplate.R
\name{generate_ora_boilerplate}
\alias{generate_ora_boilerplate}
\title{Generate ORA boilerplate code}
\usage{
generate_ora_boilerplate(
  gene_list_name = "sig_genes",
  background_name = "background_genes",
  species = "Homo sapiens",
  category = "H",
  subcategory = NULL,
  min_size = 15,
  max_size = 500,
  output_name = "ora_results"
)
}
\arguments{
\item{gene_list_name}{Character string specifying the name of the vector
containing genes of interest (e.g., significant genes). Default: "sig_genes".}

\item{background_name}{Character string specifying the name of the vector
containing background genes (e.g., all measured genes). Default: "background_genes".}

\item{species}{Character string specifying the species for msigdbr. Common
values: "Homo sapiens", "Mus musculus". Default: "Homo sapiens".}

\item{category}{Character string specifying the msigdbr category. Common
values: "H" (hallmark), "C2" (curated), "C5" (ontology), "C7" (immunologic).
Default: "H".}

\item{subcategory}{Character string specifying the msigdbr subcategory, or
\code{NULL} for all subcategories. Examples: "CP:KEGG", "CP:REACTOME",
"GO:BP", "GO:MF", "GO:CC". Default: \code{NULL}.}

\item{min_size}{Minimum gene set size for filtering. Default: 15.}

\item{max_size}{Maximum gene set size for filtering. Default: 500.}

\item{output_name}{Character string specifying the name for the output
results data frame. Default: "ora_results".}
}
\value{
Returns \code{invisible(NULL)}. The function prints code to the console
which can be copied into an analysis script.
}
\description{
Outputs boilerplate R code for performing Over-Representation Analysis (ORA)
using fgsea::fora() with gene sets from msigdbr. Uses the hypergeometric test
to identify enriched gene sets.
}
\details{
The generated code follows a standard fgsea ORA workflow:

\enumerate{
\item Retrieve gene sets from msigdbr for the specified species and category
\item Convert to fgsea-compatible list format
\item Run \code{fgsea::fora()} with hypergeometric test
\item Clean column names to snake_case with consistent naming (p_value,
adj_p_value, etc.)
\item Add an \code{enrichment_score} column (log2 odds ratio) for compatibility
with plotting functions
\item Generate summary statistics
}

The output includes an \code{enrichment_score} column (log2 of the odds ratio)
to allow use with the same clustering and plotting functions as GSEA results
(\code{\link[=cluster_enrichment_results]{cluster_enrichment_results()}}, \code{\link[=plot_enrichment_bars]{plot_enrichment_bars()}}).
}
\section{msigdbr Categories}{

\describe{
\item{H}{Hallmark gene sets (50 sets, broadly applicable)}
\item{C2}{Curated gene sets (includes KEGG, Reactome, BioCarta, etc.)}
\item{C5}{Ontology gene sets (GO Biological Process, Molecular Function,
Cellular Component)}
\item{C7}{Immunologic signature gene sets}
\item{C8}{Cell type signature gene sets}
}
}

\examples{
# Generate boilerplate for hallmark gene sets (human)
generate_ora_boilerplate()

# Use Reactome pathways
generate_ora_boilerplate(
  category = "C2",
  subcategory = "CP:REACTOME"
)

# Mouse GO Biological Process
generate_ora_boilerplate(
  species = "Mus musculus",
  category = "C5",
  subcategory = "GO:BP"
)

# Customise input names
generate_ora_boilerplate(
  gene_list_name = "upregulated_genes",
  background_name = "all_detected_genes"
)
}
