% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_type_enrichment.R
\name{plot_celltype_effect_sizes}
\alias{plot_celltype_effect_sizes}
\title{Plot effect size distributions by cell type marker set}
\usage{
plot_celltype_effect_sizes(
  marker_genes,
  limma_results,
  effect_col = "hedges_g",
  cluster_col = "cluster",
  gene_col = "gene",
  gene_symbol_col = "gene_symbol",
  x_label = NULL,
  title = "Effect size distribution by cell type marker set"
)
}
\arguments{
\item{marker_genes}{Data frame of \strong{filtered} marker genes, as returned by
\code{\link[=filter_marker_genes]{filter_marker_genes()}}. Must contain at least \code{cluster_col} and
\code{gene_col}.}

\item{limma_results}{Data frame of limma differential abundance results
containing at least \code{gene_symbol_col} and \code{effect_col}.}

\item{effect_col}{Column in \code{limma_results} containing the effect size to
plot. Default: \code{"hedges_g"}. Use \code{"log_fc"} for log2 fold change.}

\item{cluster_col}{Column in \code{marker_genes} containing cell type labels.
Default: \code{"cluster"}.}

\item{gene_col}{Column in \code{marker_genes} containing gene symbols. Default:
\code{"gene"}.}

\item{gene_symbol_col}{Column in \code{limma_results} containing gene symbols.
Default: \code{"gene_symbol"}.}

\item{x_label}{Label for the x-axis. If \code{NULL} (default), a label is
derived automatically from \code{effect_col}: \code{"hedges_g"} becomes
\code{"Hedges' g"}, \code{"log_fc"} becomes \code{"log2 fold change"}.}

\item{title}{Plot title. Default: \code{"Effect size distribution by cell type marker set"}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a violin and boxplot for each cell type, showing the distribution of
per-protein effect sizes (e.g. Hedges' g or log2 fold change) across that
cell type's detected marker proteins. The join between marker genes and limma
results is performed internally.
}
\details{
Proteins with \code{NA} or multi-mapped gene symbols (containing \code{";"}) are
excluded. Where the same gene symbol appears more than once in
\code{limma_results}, the first occurrence is kept.

Cell types are ordered on the y-axis by their median effect size (ascending).
}
\examples{
\dontrun{
plot_celltype_effect_sizes(markers_filtered, limma_results)
plot_celltype_effect_sizes(markers_filtered, limma_results, effect_col = "log_fc")
}

}
\seealso{
\code{\link[=filter_marker_genes]{filter_marker_genes()}}, \code{\link[=plot_gsea_bars]{plot_gsea_bars()}}
}
