% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/complete_cases.R
\name{get_greedy_complete_cases}
\alias{get_greedy_complete_cases}
\title{Extract a joint complete-cases sub-matrix at a chosen operating point}
\usage{
get_greedy_complete_cases(
  abundance,
  n_proteins = NULL,
  n_samples = NULL,
  pct_samples = NULL
)
}
\arguments{
\item{abundance}{Numeric matrix or data frame with proteins as rows
and samples as columns. Column names are required.}

\item{n_proteins}{Target minimum number of jointly-complete
proteins. The function returns the operating point with the
\emph{largest} sample retention that achieves at least this protein
count.}

\item{n_samples}{Target number of samples to retain. The trace
evaluates one operating point per \code{n_samples_kept} value, so this
maps directly to a single row of the trace.}

\item{pct_samples}{Target percentage of samples to retain.
Converted to \code{round(ncol(abundance) * pct_samples / 100)} and
treated as \code{n_samples}.}
}
\value{
A numeric matrix or data frame with proteins as rows and
samples as columns. Rows are restricted to proteins with no
missing values across the kept samples; columns are the kept
samples chosen by greedy + 2-opt.
}
\description{
Runs the greedy + 2-opt joint complete-cases trace (see
\code{\link[=plot_greedy_complete_cases_distribution]{plot_greedy_complete_cases_distribution()}}) and returns the
sub-matrix at the operating point matching the requested target.
Specify exactly one of \code{n_proteins}, \code{n_samples}, or \code{pct_samples}.
}
\details{
Finding the best \emph{k} samples to drop is NP-hard so this function
is a heuristic and provides no optimality guarantee. The greedy +
2-opt approach is described in
\code{\link[=plot_greedy_complete_cases_distribution]{plot_greedy_complete_cases_distribution()}}.
}
\examples{
\dontrun{
# Largest sample subset achieving >= 1000 jointly-complete proteins
sub <- get_greedy_complete_cases(abundance, n_proteins = 1000)

# Drop down to a fixed number of samples
sub <- get_greedy_complete_cases(abundance, n_samples = 200)

# ... or to a percentage
sub <- get_greedy_complete_cases(abundance, pct_samples = 80)
}
}
\seealso{
\code{\link[=plot_greedy_complete_cases_distribution]{plot_greedy_complete_cases_distribution()}}.
}
